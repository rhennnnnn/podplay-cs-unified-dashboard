NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
HUBSPOT_PRIVATE_APP_TOKEN=
GOOGLE_SHEETS_CLIENT_EMAIL=
GOOGLE_SHEETS_PRIVATE_KEY=
MRP_SHEET_ID=
MRP_SHEET_TAB=

CRON_SECRET=

# Closed Locations reminders — posts to #cs-team-daily when a reminder is due,
# @mentioning whoever set it. Bot token needs scopes: chat:write and
# users:read.email; invite the bot to the channel. Optional: if either is unset
# the in-app reminder flag still works, only the Slack post is skipped.
SLACK_BOT_TOKEN=
SLACK_TEAM_DAILY_CHANNEL_ID=
